<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="autenticacion">
    <select id="login" resultType="modelo.pojo.Usuario" parameterType="modelo.pojo.Usuario">
        SELECT nombre, apellidoPaterno, apellidoMaterno, CURP, correoElectronico, username, password,rolID
        FROM usuario
        WHERE username = #{username} AND password = #{password}   
    </select>
</mapper>
