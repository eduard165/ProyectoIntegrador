<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="usuario">
<insert id="agregarUsuario" parameterType="modelo.pojo.Usuario">
    INSERT INTO Usuario (nombre, apellidoPaterno, apellidoMaterno, CURP, correoElectronico, username, password, rol, empresaRFC)
    VALUES (#{nombre}, #{apellidoPaterno}, #{apellidoMaterno}, #{CURP}, #{correoElectronico}, #{username}, #{password}, #{rol}, #{empresaRFC})
</insert>
<update id="editarUsuario" parameterType="modelo.pojo.Usuario">
    UPDATE Usuario
    SET nombre = #{nombre},
    apellidoPaterno = #{apellidoPaterno},
    apellidoMaterno = #{apellidoMaterno},
    correoElectronico = #{correoElectronico},
    password = #{password},
    empresaRFC = #{empresaRFC}
    WHERE idUsuario = #{idUsuario}
</update>
<delete id="eliminarUsuario" parameterType="Integer">
    DELETE FROM Usuario
    WHERE idUsuario = #{idUsuario}
</delete>
<select id="buscarUsuario" resultType="modelo.pojo.Usuario" parameterType="String">
    SELECT *
    FROM Usuario
    WHERE nombre LIKE CONCAT('%', #{param}, '%')
    OR username LIKE CONCAT('%', #{param}, '%')
    OR rol LIKE CONCAT('%', #{param}, '%')
</select>


</mapper>
